// eslint-disable-next-line @typescript-eslint/no-unused-vars
namespace OSFramework.Enum {
    export enum RowHeader {
        Empty = 1,
        RowCheckbox = 2,
        RowNumber = 3,
        RowNumberAndCheckbox = 4
    }
}
