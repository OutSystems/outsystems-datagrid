// eslint-disable-next-line @typescript-eslint/no-unused-vars
namespace Features {
    export enum PageLabel {
        PageCount = 'PageCount',
        PageIndex = 'PageIndex',
        PageSize = 'PageSize',
        RowEnd = 'RowEnd',
        RowStart = 'RowStart',
        RowTotal = 'RowTotal'
    }
}
